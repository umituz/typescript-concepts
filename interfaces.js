// function todo(todo: { title: string, text: string }) {
//     console.log(todo.title + " " + todo.text);
// }
function todo(todo) {
    console.log(todo.title + " " + todo.text);
}
var myTodo = {
    title: "Trash",
    text: "Take out trash!"
};
todo(myTodo);
